[project]
name = "combinational-podem-atpg-v1"
version = "0.1.0"
description = "Combinational PODEM ATPG for ISCAS'85 benchmarks"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
